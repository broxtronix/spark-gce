# Default system properties included when running spark-submit.
# This is useful for setting default environmental settings.

# Example:
# spark.master                     spark://master:7077
# spark.eventLog.enabled           true
# spark.eventLog.dir               hdfs://namenode:8021/directory
# spark.serializer                 org.apache.spark.serializer.KryoSerializer
# spark.driver.memory              5g
# spark.executor.extraJavaOptions  -XX:+PrintGCDetails -Dkey=value -Dnumbers="one two three"

spark.executor.extraLibraryPath $HOME/ephemeral-hdfs/lib/native/
spark.executor.extraClassPath   $HOME/ephemeral-hdfs/conf

spark.driver.memory 30g
spark.executor.memory 90g
spark.driver.maxResultSize = 10g
spark.akka.frameSize = 2047
spark.rdd.compress = true
spark.kryoserializer.buffer.max.mb = 2047
